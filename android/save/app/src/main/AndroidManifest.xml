<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a16636.save">

    <application
        android:allowBackup="true"
        android:icon="@drawable/timg"
        android:label="the money"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main"></activity>
        <activity android:name=".jisuanqi"></activity>
        <activity android:name=".beiwan"></activity>
        <activity android:name=".Modify"></activity>
        <activity android:name=".BillActivity"></activity>
    </application>

</manifest>