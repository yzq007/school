<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.zptc.demo1204">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".tongActivity"></activity>
        <activity android:name=".xiaoActivity"></activity>
        <activity android:name=".myActivity"></activity>
        <activity android:name=".xinActivity"></activity>
        <activity android:name=".weiActivity"></activity>
        <activity android:name=".qianActivity"></activity>
    </application>

</manifest>